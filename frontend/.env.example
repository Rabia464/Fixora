# Fixora Frontend — copy to .env.local for local development.
# Base URL of the FastAPI backend. Defaults to http://localhost:8000/api/v1
# in the API client when unset.
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
