# ===========================================================
# Fixora – Backend Production Dependencies
# Python 3.12+
# Install: pip install -r requirements.txt
# ===========================================================

# ── Web Framework ───────────────────────────────────────────
fastapi==0.111.1
uvicorn[standard]==0.30.1          # ASGI server with watchfiles + httptools

# ── Data Validation & Settings ──────────────────────────────
pydantic==2.7.4
pydantic[email]==2.7.4             # EmailStr support (used in user schemas)
pydantic-settings==2.3.4           # Reads .env into typed Settings model

# ── Database ────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.31        # ORM + async engine support
asyncpg==0.29.0                    # Async PostgreSQL driver (used by SQLAlchemy)
alembic==1.13.2                    # Schema migration tool

# ── Authentication ───────────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT creation and verification (HS256)

# ── Utilities ───────────────────────────────────────────────
python-dotenv==1.0.1               # .env file loading (backup for pydantic-settings)
